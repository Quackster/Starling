plugins {
    id 'base'
}

group = 'org.starling'
version = '1.0-SNAPSHOT'

subprojects {
    group = rootProject.group
    version = rootProject.version

    repositories {
        mavenCentral()
        maven { url = 'https://jitpack.io' }
    }
}

tasks.named('build') {
    dependsOn(':starling-server:build', ':starling-web:build')
}

tasks.named('assemble') {
    dependsOn(':starling-server:packageDist', ':starling-web:packageDist')
}

tasks.register('packageDist') {
    group = 'build'
    description = 'Packages the Starling server and web distributions.'
    dependsOn(':starling-server:packageDist', ':starling-web:packageDist')
}
